{
    "name": "cdkworkshop.com",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "tsc --build && (cd ../workshop && hugo)",
        "watch": "tsc --build -w",
        "deploy": "npm run build && cdktf deploy",
        "synth": "npm run build && npx cdktf synth"
    },
    "devDependencies": {
        "@biomejs/biome": "1.8.3",
        "@types/node": "^24.0.14",
        "cdktf": "^0.21.0",
        "cdktf-cli": "^0.21.0",
        "constructs": "^10.4.2",
        "terraconstructs": "^0.1.0",
        "typescript": "^5.5.3"
    },
    "repository": {
        "url": "https://github.com/terraconstructs/intro-workshop.git",
        "type": "git"
    },
    "dependencies": {
        "cdktf": "^0.21.0",
        "constructs": "^10.4.2",
        "terraconstructs": "^0.1.0"
    },
    "engines": {
        "node": ">= 20.0.0"
    }
}
